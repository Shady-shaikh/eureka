$(document).ready(function(){$(".file-repeater, .contact-repeater, .repeater-default").repeater({
	show:function(setIndexes){
	$(this).slideDown();
	console.log(setIndexes);
	console.log($(this));
	$(this).find('.modal').attr('id','batch_modal_');
	inittypeahead();
},hide:function(e){
	confirm("Are you sure you want to delete this element?")&&$(this).slideUp(e)
setTimeout(function(){ calculate_grand_total(); }, 500);
	inittypeahead();	
},
ready: function (setIndexes) {
       console.log(setIndexes);

}
})});